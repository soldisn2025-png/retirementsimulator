import React from "react";
import { createRoot } from "react-dom/client";
import RetireMap from "./RetireMap.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <RetireMap />
  </React.StrictMode>
);
